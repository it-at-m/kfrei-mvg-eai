{
  "local-dev": {
    "Security": {
      "Auth": {
        "keycloak": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Username": "{{USERNAME}}",
          "Password": "{{PASSWORD}}",
          "Client ID": "{{CLIENT_ID}}",
          "Client Secret": "{{CLIENT_SECRET}}",
          "Token URL": "{{TOKEN_URL}}"
        }
      }
    }
  }
}